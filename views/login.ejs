<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Aligram</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen font-sans text-gray-800 bg-gray-100">
  <header class="bg-teal-900 text-white shadow-lg p-4 text-center">
    <h1 class="text-2xl md:text-3xl font-bold">Aligram</h1>
  </header>
  <main class="container mx-auto p-4">
    <div class="max-w-md mx-auto bg-white p-6 rounded-lg shadow-md">
      <h1 class="text-2xl font-semibold text-teal-900 mb-6 text-center">Login</h1>
      <% if (errors.length > 0) { %>
        <ul class="mb-4">
          <% errors.forEach(error => { %>
            <li class="text-red-500"><%= error %></li>
          <% }) %>
        </ul>
      <% } %>
      <form action="/login" method="post" class="space-y-4">
        <div>
          <label for="email" class="block font-semibold mb-1">Email:</label>
          <input type="email" id="email" name="email" required class="w-full p-2 border rounded-md">
        </div>
        <div>
          <label for="password" class="block font-semibold mb-1">Password:</label>
          <input type="password" id="password" name="password" required class="w-full p-2 border rounded-md">
        </div>
        <button type="submit" class="w-full bg-teal-500 text-white p-2 rounded-md hover:bg-teal-700 transition">LOGIN</button>
      </form>
      <a href="/register" class="block text-center mt-4 text-teal-500 hover:underline">Don't have an account? Register</a>
    </div>
  </main>
</body>
</html>