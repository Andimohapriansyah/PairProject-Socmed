<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= post.title %> - Aligram</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen font-sans text-gray-800 bg-gray-100">
  <header class="bg-teal-900 text-white shadow-lg p-4 text-center">
    <h1 class="text-2xl md:text-3xl font-bold">Aligram</h1>
  </header>
  <main class="container mx-auto p-4">
    <div class="max-w-lg mx-auto bg-white p-6 rounded-lg shadow-md">
      <h1 class="text-2xl font-semibold text-teal-900 mb-4"><%= post.title %></h1>
      <img src="<%= post.imgUrl || 'https://via.placeholder.com/300' %>" alt="<%= post.title %>" class="w-full rounded-md mb-4">
      <p class="text-gray-600 mb-2"><%= post.content %></p>
      <p class="text-sm text-gray-500 mb-1"><strong>By:</strong> <%= post.User.email %></p>
      <p class="text-sm text-gray-500 mb-1"><strong>Tags:</strong> <%= post.Tags.map(tag => tag.name).join(', ') %></p>
      <p class="text-sm text-gray-500 mb-4"><strong>Posted on:</strong> <%= formatDate(post.createdAt) %></p>
      <a href="/posts" class="block text-center text-teal-500 hover:underline">Back to Posts</a>
    </div>
  </main>
</body>
</html>